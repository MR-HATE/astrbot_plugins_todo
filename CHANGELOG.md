# 变更记录

本项目按里程碑开发，每个里程碑都在真机上验收通过后才进入下一个。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## v1.0.4
- 修复：**定时任务唤起时误报「尚未绑定 Microsoft 账号」**。
  账号 key 是「平台类型:用户ID」（如 `aiocqhttp:1747831170`），但定时任务这条路
  有两处会把它算错，导致到点后工具找不到凭据（凭据其实完好）：
  1. AstrBot 用合成事件 `CronMessageEvent` 唤醒 Agent，其 `PlatformMetadata.name`
     被写死为 `cron`；
  2. `unified_msg_origin` 的第一段是平台**实例 id**（配置里的 `id`，如 `yume`），
     不是平台**类型**（`aiocqhttp`）——`MessageSession.platform_name` 自 v4.0.0 起
     实际存的是 `platform_id`。

  现在合成事件下会用实例 id 去平台注册表反查 `meta().name` 得到类型；查不到才退回实例 id。
  新建的定时任务还会把原始 aid 直接写进 payload（双保险）。**已存在的定时任务无需重建。**
- 修复：同一处思维定势导致的**提醒被静默跳过**。判断"平台是否支持主动消息"原本也是
  拿 umo 第一段去比类型白名单，实例 id 不在白名单里就直接不建提醒。现在改为询问适配器
  自己声明的 `meta().support_proactive_message`，白名单只作兜底。
- 测试：fixture 里的平台**实例 id 与类型刻意取不同值**（`yume` / `aiocqhttp`），
  并把测试目录的包名改为从目录名推导（`plugin_pkg` 别名），
  避免"目录改名就整套跑不起来"和"fixture 恰好对上所以假绿"。

## v1.0.1
- 使用ruff对代码进行了静态检查，修复了部分代码风格问题，提升了代码可读性和可维护性。

## v1.0.0

首个完整版本：把自然语言计划变成 Microsoft To Do 里的待办，并在到点时由机器人主动提醒。

### 账号与授权

- OAuth 2.0 设备码流程（Device Code Flow）：**服务器无需浏览器、无需公网回调**，
  用户在手机或任意电脑上输码即可完成绑定
- 每个用户各自绑定，凭据按「平台 + 发送者ID」隔离存储于 AstrBot 插件级 KV
- access_token 到期前自动续期；refresh_token 每次刷新后覆盖保存；
  失效时删除本地凭据并明确提示重新授权（不静默失败）
- 授权成功后在原会话主动通知

### 待办导入

- 自然语言 → 结构化待办 → **预览确认** → 写入指定列表（两阶段提交）
- 目标列表按计划名自动创建/复用；可指定已有列表
- 时间归一化：`今天/明天/后天/N天后/下周三/9月18日/12-01` 等相对与绝对说法；
  中文数字时刻（`下午三点`、`九点半`）；带时区偏移的输入自动换算到配置时区
- 优先级中英口语（`重要/紧急/很低/urgent`）映射到 high/normal/low
- 子步骤写入 To Do 的「步骤」（checklist）
- **跨机器幂等**：去重同时看本地记录与 To Do 现有任务，
  换机器、重装、甚至用户自己手动建过同一条都不会重复导入
- 批量写入走 Graph `$batch`（每批 ≤20），把 N 次往返压成 1 次

### 查询与增删改

- `ms_todo_list_tasks`：读取待办与**完成状态**（⬜/🔄/✅/⏳/💤），
  支持 `pending`（默认）/`today`/`overdue`/`upcoming`/`completed`/`all` 六种范围
- 改期（支持相对日期，保留原时刻）、改优先级/标题/备注、清除截止时间
- 标记完成 / 取消完成
- 删除单条任务、**删除整个列表**（连同其中任务）——均需二次确认
- 给已有待办追加子步骤
- 按标题模糊定位；匹配到多条时返回候选交由用户选择，**绝不自己猜**

### 定时提醒

- 复用 AstrBot 内置定时任务创建 `active_agent` 作业，到点在原会话主动提醒
- 一次性提醒用 `run_once` + `run_at`；重复提醒（每天/每周/每月）转成 5 段 crontab
- 任务标记完成、删除任务、删除列表时**自动取消**对应提醒
- 平台不支持主动消息时降级为仅 To Do 提醒，并明确告知用户
- 可在 WebUI「未来任务」页查看或手动取消

### Agent 协作

- 12 个 FunctionTool，参数用完整 JSON Schema 约束模型输出
- 内置 Skill `ms-todo-import`：触发条件、拆分粒度、时间规则、确认流程、反例清单
- `on_llm_request` 规则兜底注入：未启用「使用电脑能力」（读不到 Skill 正文）时，
  关键规则仍以固定文本追加到 system prompt，且保证同一次请求只注入一次

### 工程

- 96 个单元测试（零依赖运行器，假 Graph / 假定时任务 / 内存 KV）
- 手动端到端验收清单 `tests/manual-checklist.md`（真实授权、真实界面、到点提醒）
- 零依赖规范自检脚本 `scripts/lint_check.py`（未使用 import / 行宽 / 行尾空白 / 裸 except）

### 已知限制

- 只有 `Tasks.ReadWrite` 委派权限，必须人工授权一次；**世纪互联（21Vianet）版
  Microsoft 365 不支持 To Do API**
- 不支持重复任务（重复只作用于提醒）、不支持移动任务到其它列表、不支持重命名列表
- 在 WebUI 的 Chat 页使用需要单独绑定（账号按平台隔离）

